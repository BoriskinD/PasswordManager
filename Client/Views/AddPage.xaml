<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Client.AddPage"
             Title="AddPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ImageButton Source="{Binding ImagePath}" Grid.Row="0"
                     Margin="0,5,0,0" Command="{Binding SelectImageCommand}"/>

        <VerticalStackLayout Margin ="5,10,0,0" Grid.Row="1">
            <Entry Placeholder="Название приложения"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   Margin="5" Text="{Binding Title}"/>

            <Entry Placeholder="Имя пользователя"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   Margin="5" Text="{Binding UserLogin}"/>

            <Entry Placeholder="Пароль"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   Margin="5" Text="{Binding UserPassword}"/>

            <Button Text="Сохранить" HorizontalOptions="Center" 
                    VerticalOptions="Center" Command="{Binding SaveCommand}"
                    Margin="0,5,0,5"/>
        </VerticalStackLayout>
    </Grid>
</ContentPage>